# 이 파일을 .env로 복사한 뒤 실제 값으로 수정하세요: cp .env.example .env

# Server
PORT=8008

# Database (PostgreSQL)
# docker-compose로 postgres 실행 시 아래 값을 사용합니다.
DB_HOST=localhost
DB_PORT=5436
DB_USER=postgres
DB_PASSWORD=test-bhlee
DB_NAME=biocom

# 개발 단계: synchronize=true (기본값). 프로덕션에서 false로: DB_SYNCHRONIZE=false

# JWT (admin 로그인용)
JWT_SECRET=your_jwt_secret_change_in_production
JWT_EXPIRES_IN=7d

# 예약 링크 베이스 URL
APP_URL=http://localhost:3000

# SMTP (이메일 발송, Gmail 예시)
# TODO: Gmail: 2단계 인증 활성화 후 앱 비밀번호 발급 필요 (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hae8064@gmail.com
SMTP_PASS=your-app-password